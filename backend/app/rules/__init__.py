"""Rules package — reserved for Phase 2+.

Diagnostic rule engines (e.g. cash-jam detection rules per model) will be
registered here, model-by-model, following the same plugin pattern as the
adapters registry. Deliberately empty in Phase 1.
"""
